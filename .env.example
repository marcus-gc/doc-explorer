# GitHub token for fetching source files from the private repo.
# Only needed if LOCAL_REPO_ROOT is not set.
GITHUB_TOKEN=
GITHUB_OWNER=givecampus
GITHUB_REPO=givecampus
GITHUB_REF=staging

# Path to a local clone of the givecampus repo.
# When set, source files are read from disk instead of the GitHub API.
# LOCAL_REPO_ROOT=/path/to/givecampus

# Subdirectory in the repo where markdown docs live (default: docs)
DOCS_PATH=docs
